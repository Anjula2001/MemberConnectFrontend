"use client"; import { useRef, useState } from "react"; import { Button } from "../../../../components/ui/button"; import Grade5Form, { Grade5FormRef, } from "../../../../components/ui/grade5schoolarship/grade5form"; import DocumentUpload from "../../../../components/ui/grade5schoolarship/documentupload"; import { MarkIncompleteModal } from "../../../../components/ui/grade5schoolarship/MarkIncomplete"; export default function Grade5ScholarshipPage() { const formRef = useRef<Grade5FormRef>(null); const [openModal, setOpenModal] = useState(false); const [status, setStatus] = useState("Draft"); const [incompleteReason, setIncompleteReason] = useState(""); const handleConfirm = (reason: string) => { if (!reason.trim()) return; setStatus("Incomplete"); setIncompleteReason(reason); setOpenModal(false); console.log("Marked as Incomplete:", reason); }; const handleSave = () => { formRef.current?.submitForm(); }; const handleSubmitForm = () => { formRef.current?.submitForm(); }; return ( <> <div className="flex flex-1 flex-col gap-4 p-4 pt-0"> <div className="min-h-[100vh] flex-1 rounded-xl bg-muted/50 p-6"> {/* Header */} <div className="flex items-center justify-between"> <p className="text-2xl font-bold text-[#953002]"> New Grade 5 Scholarship </p> <div className="flex gap-2"> <Button onClick={handleSave} className="bg-white text-black hover:bg-gray-100" > Save </Button> <Button onClick={() => setOpenModal(true)} className="bg-[#D4183D] text-white hover:bg-[#b31533]" > Mark Incomplete </Button> <Button onClick={handleSubmitForm} className="bg-[#953002] text-white hover:bg-[#672102]" > Submit </Button> </div> </div> <div className="flex gap-6 mt-6"> <div className="flex-1 flex flex-col gap-6"> <div className="bg-white rounded-lg shadow-sm p-6"> <Grade5Form ref={formRef} /> </div> <div className="bg-white rounded-lg shadow-sm p-4"> <div className="flex items-center justify-between mb-2"> <p className="text-xl font-bold text-[#953002]"> Fund Disbursement </p> <Button className="bg-gray-50 text-black hover:bg-gray-100"> Refresh </Button> </div> </div> </div> <div className="w-1/4"> <DocumentUpload /> </div> </div> </div> </div> <MarkIncompleteModal open={openModal} onClose={() => setOpenModal(false)} onConfirm={handleConfirm} /> </> ); }